{% for field in _data %}
{% if field.Gui | length %}
    {% if field.Type == "object" %}

    {% elif field.Type == "array" %}
        {{ field.Gui.Name }}: {
            type: "array",
            items: {

            }
        }
    {% else %}
        {{ field.Gui.Name }}: { 
            "type": "{{ field.Gui.Type }}",
            "name": "{{ field.Gui.Label }}",
            "required": {{ "true" if field.Gui.Required else "false" }}
        },
    {% endif %}
{% endif %}
{% endfor %}