import * as mocha from 'mocha';

var assert = require('assert');
var qz = require('../src/index');
var lo = require('lodash');
var path = require('path');
var fs = require('fs');

mocha.describe('Exec', function() {
    mocha.it('Should show help', function(done) {
        done();
    });
});